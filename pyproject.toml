[project]
name = "model-metadata"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.12.0",
    "requests>=2.32.0",
]

[dependency-groups]
dev = [
    "prek>=0.3.5",
    "ruff>=0.15.5",
]
